name: pune-platter-co
static_sites:
  - name: web
    environment_slug: html
    source_dir: /
    routes:
      - path: /
    catchall_document: index.html
    github:
      repo: YOUR_GITHUB_USERNAME/YOUR_REPO_NAME
      branch: main
      deploy_on_push: true
